name: Deploy to Firebase Hosting

on:
  pull_request:
    branches:
      - main  # Auto-deploy preview for PRs targeting main

  workflow_dispatch:
    inputs:
      environment:
        description: 'Where to deploy'
        required: true
        default: 'preview'
        type: choice
        options:
          - preview
          - production

permissions:
  checks: write
  contents: read  # required by actions/checkout

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install frontend dependencies
      run: npm install

    - name: Build React app with Firebase config
      run: npm run build
      env:
        VITE_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
        VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
        VITE_FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID }}
        VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
        VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
        VITE_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
        VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID }}
        VITE_BACKEND_URL: ${{ secrets.VITE_BACKEND_URL }}

    - name: Deploy to Firebase Preview (PR or manual)
      if: github.event_name == 'pull_request' || (github.event_name == 'workflow_dispatch' && inputs.environment == 'preview')
      uses: FirebaseExtended/action-hosting-deploy@v0
      with:
        repoToken: ${{ github.token }}
        firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        projectId: my-smart-shopper-e0c23
        entryPoint: .
        channelId: ${{ github.event_name == 'pull_request' && format('pr-{0}', github.event.pull_request.number) || format('preview-{0}', github.run_number) }}
        target: live
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

    - name: Deploy to Firebase Production (manual or main)
      if: (github.event_name == 'workflow_dispatch' && inputs.environment == 'production') || (github.event_name != 'pull_request' && github.ref == 'refs/heads/main')
      uses: FirebaseExtended/action-hosting-deploy@v0
      with:
        repoToken: ${{ github.token }}
        firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        projectId: my-smart-shopper-e0c23
        entryPoint: .
        channelId: live
        target: live
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

    - name: Test deployed Gemini endpoint
      run: |
        if [[ "${{ github.event_name }}" == "pull_request" ]]; then
          BASE_URL="https://pr-${{ github.event.pull_request.number }}--my-smart-shopper-e0c23.web.app"
        elif [[ "${{ github.event_name }}" == "workflow_dispatch" && inputs.environment == "preview" ]]; then
          BASE_URL="https://preview-${{ github.run_number }}--my-smart-shopper-e0c23.web.app"
        else
          BASE_URL="https://my-smart-shopper-e0c23.web.app"
        fi

        echo "🔄 Pinging $BASE_URL/api/autoMapItems"
        RESPONSE=$(curl -s -X POST "$BASE_URL/api/autoMapItems" \
          -H "Content-Type: application/json" \
          -d '{"prompt": "test"}')

        echo "🔍 Response:"
        echo "$RESPONSE"

        if echo "$RESPONSE" | grep -q 'candidates'; then
          echo "✅ API is working as expected"
        else
          echo "❌ Unexpected or missing response from API"
          exit 1
        fi
